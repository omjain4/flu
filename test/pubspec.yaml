name: test
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^4.8.0
  firebase_core: ^2.15.0
  cloud_firestore: ^4.17.5
  mobile_scanner: ^6.0.6
  barcode_scan2: ^4.2.0
  http: ^1.3.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/barcode_scanning_image.jpg
